name: Or Condition Demo
on: workflow_dispatch
jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Run for Tete or fotso94
        if: github.actor == 'Tete' || github.actor == 'fotso94'
        run: echo "It is triggered by either Tete or fotso94"
